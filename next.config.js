/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'picsum.photos', 'res.cloudinary.com', 'picsum.photos'],
  },
}

module.exports = nextConfig
